server:
  port: 9090

spring:
  rabbitmq:
    host: 10.44.0.20
    port: 5672
    username: user
    password: user
  zipkin:
    enabled: true
    base-url: ${ZIPKIN_URL}
    service:
      name: auth-service
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: root
    url: jdbc:postgresql://10.44.0.13:5432/AuthDB
  jpa:
    hibernate:
      ddl-auto: update
# ÖNEMLİ: Şifre gibi başkalarının görmesini istemeyeceğiniz verileri yml dosyasında dahi tutmak sakıncalıdır.
# Bu nedenle bu tür bilgilerin gizlenmesi gereklidir.
# Bu gizleme işlemi için Environment'lar(Ortam değişkenleri) kullanılmalıdır.
authservices:
  secrets:
    secret-key: ${JAVA9_SECRETKEY}
    issuer: ${JAVA9_ISSUER}
